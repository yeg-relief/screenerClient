import { Component } from '@angular/core'
import { Store } from '@ngrx/store';
import { AppState } from '../../reducers';

@Component({
  template: `
    <h1>THIS IS KEY ADD </h1>
  `
})
export class KeyEditorAdd{
  
}