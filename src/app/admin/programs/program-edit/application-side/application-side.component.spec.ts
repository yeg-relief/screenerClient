/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ApplicationSideComponent } from './application-side.component';

describe('Component: ApplicationSide', () => {
  it('should create an instance', () => {
    let component = new ApplicationSideComponent();
    expect(component).toBeTruthy();
  });
});
