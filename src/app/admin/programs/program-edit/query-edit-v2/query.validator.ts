import { ValidatorFn, AbstractControl } from '@angular/forms';
import { ProgramCondition, Key } from '../../../models';

export function queryValidator(control: AbstractControl): {[key: string]: any} {
   return null;
}